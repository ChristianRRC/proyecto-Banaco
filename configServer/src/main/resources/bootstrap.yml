server:
  port: 3000

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ChristianRRC/proyecto-Banco.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user: root
    password: s3cr3t



